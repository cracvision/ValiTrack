

## Increase Auth Logo Size

**File: `src/pages/Auth.tsx`** (line 57)

Change the logo class from `h-16 w-16` to `h-28 w-28` (112×112px). This nearly doubles the current size while staying well within the `max-w-md` card width. If you want to go even larger, `h-32 w-32` (128px) is the maximum before it starts to feel disproportionate relative to the card.

Single line change — no other files affected.

